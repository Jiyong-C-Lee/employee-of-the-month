# wrangler 4.x는 @cloudflare/workers-types v5를 peerOptional로 원하지만, 워커 테스트를 돌리는
# @cloudflare/vitest-pool-workers 0.5.x는 내부에 wrangler 3을 두고 v4를 요구한다. 둘 다 optional
# peer라 실제 런타임에는 영향이 없다 — 설치만 막는다. 상위 워크스페이스도 같은 조합으로 돈다.
legacy-peer-deps=true
